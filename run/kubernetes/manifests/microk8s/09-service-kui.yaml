apiVersion: v1
kind: Service
metadata:
  name: kui
  namespace: kui
  labels:
    app.kubernetes.io/name: kui
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: kui
