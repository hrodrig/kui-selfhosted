apiVersion: v1
kind: Service
metadata:
  name: kiko
  namespace: kui
  labels:
    app.kubernetes.io/name: kiko
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: kiko
