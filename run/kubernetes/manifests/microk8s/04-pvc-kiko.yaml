apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kiko-data
  namespace: kui
  labels:
    app.kubernetes.io/name: kiko
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
